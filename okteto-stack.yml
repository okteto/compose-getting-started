name: voting-app

endpoints:
  - path: /
    service: vote
    port: 8080

services:
  vote:
    build: vote
    replicas: 2
    ports:
      - 8080

  redis:
    image: redis
    ports:
      - 6379
    volumes:
      - redis:/data

volumes:
  redis:
    size: 1Gi
